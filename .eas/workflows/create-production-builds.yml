name: Create Production Builds

on:
  push:
    branches: ['main']

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: development
  get_build:
    needs: [build_android]
    type: get_build
    params:
      buildId: ${{ needs.build_android.outputs.buildId }}
      outputPath: bin/
